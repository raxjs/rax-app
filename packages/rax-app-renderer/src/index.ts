import raxAppRenderer from './renderer';
import { getInitialData, setInitialData } from './initialData';
import { RenderAppConfig } from './types';

export * from './initialData';

export { getInitialData, setInitialData, RenderAppConfig };

export default raxAppRenderer;
