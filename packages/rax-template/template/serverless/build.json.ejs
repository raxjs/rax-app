<%_ const isSSR = projectFeatures.includes('ssr') -%>
{
  "plugins": [
    [
      "rax-plugin-app",
      {
        "targets": ["<%- projectTargets.join('", "') %>"]
      }
    ],
    [
      "rax-plugin-faas",
      {
        "aliyunConfig": {
          "id": "<%= projectAliyunId %>",
          "region": "<%= projectServerlessRegion %>"
        },
        "functionGroup": {
          "name": "raxFaaS",
          "root": "src/api",
          "functions": {
            "user": {
              "path": "user"
            }
          }
        }
      }
    ]<%_ if (isSSR) { -%>,<%_ } %>
    <%_ if (isSSR) { -%>
    "rax-plugin-ssr"
    <%_ } -%>
  ]
}
