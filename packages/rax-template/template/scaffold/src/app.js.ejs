import { createElement } from 'rax';
import { useRouter } from 'universal-app-runtime';

export default function App(props) {
  const { routerConfig } = props;

  const { Router } = useRouter(routerConfig);

  return <Router {...props} />;
}
