<%_ const isSSR = projectFeatures.includes('ssr') -%>
{
  "routes": [
    {
      "path": "/",
      <%_ if (isSSR) { -%>
      "name": "home",<%# When SSR Project is deployed as function (FaaS), name will be used as function name %>
      <%_ } -%>
      "source": "pages/Home/index"
    }
  ],
  <%_ if (isSSR) { -%>
  "historyType": "browser",
  "hydrate": true,
  <%_ } -%>
  "window": {
    "defaultTitle": "Rax App 1.0"
  }
}
