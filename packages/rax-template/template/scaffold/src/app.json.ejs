<%_ const isSSR = projectFeatures.includes('ssr') -%>
{
  "routes": [
    {
      "path": "/",
      "source": "pages/Home/index"
    }
  ],
  <%_ if (isSSR) { -%>
  "historyType": "browser",
  <%_ } -%>
  "window": {
    "defaultTitle": "Rax App 1.0"
  }
}
