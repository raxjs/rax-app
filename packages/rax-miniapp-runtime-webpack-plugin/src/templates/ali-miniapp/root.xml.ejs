<template name="element">
  <block a:if="{{!r.nodeId}}">
    <template is="children" data="{{r: r.children}}" />
  </block>
  <block a:elif="{{r.nodeType === 'text'}}">
    {{r.content}}
  </block>
  <block a:elif="{{r.tagName === 'custom-component'}}">
    <template is="{{r.customComponentName}}" data="{{r: r}}" />
  </block>
  <block a:elif="{{r.tagName === 'miniapp-plugin'}}">
    <template is="{{r.miniappPluginName}}" data="{{r: r}}" />
  </block>
  <block a:elif="{{!r._behavior}}">
    <template a:if="{{r.tagName === 'img'}}" is="image" data="{{r: r, isHTMLElement: true}}" />
    <template a:elif="{{r.tagName === 'input'}}" is="input" data="{{r: r, isHTMLElement: true}}" />
    <template a:elif="{{r.tagName === 'textarea'}}" is="textarea" data="{{r: r, isHTMLElement: true}}" />
    <template a:elif="{{r.tagName === 'video'}}" is="video" data="{{r: r, isHTMLElement: true}}" />
    <view
      a:elif="{{r.tagName}}"
      style="{{r.style}}"
      class="h5-{{r.tagName}} {{r.class}}"
      id="{{r.id}}"
      animation="{{r.animation}}"
      data-private-node-id="{{r.nodeId}}"
      data-private-page-id="{{r.pageId}}"

      onTap="onTap"
      onTouchStart="onTouchStart"
      onTouchMove="onTouchMove"
      onTouchEnd="onTouchEnd"
      onTouchCancel="onTouchCancel"
    >
      <template is="children" data="{{r: r.children}}" />
    </view>
  </block>
  <block a:else>
    <template is="{{r._behavior}}" data="{{r: r}}" />
  </block>
</template>

<template name="children">
  <block a:for="{{r}}" a:key="nodeId">
    <template is="element" data="{{r: item}}" />
  </block>
</template>

<!--Container-->
<template name="view">
  <view
    style="{{r.style}}"
    class="{{r.class}}"
    id="{{r.id}}"
    hidden="{{r.hidden}}"
    animation="{{r.animation}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onTouchMove="onTouchMove"
    onTouchStart="onTouchStart"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"
    onLongTap="onLongTap"

    onFirstAppear="onViewFirstAppear"
    onAppear="onViewAppear"
    onDisappear="onViewDisappear"

    disable-scroll="{{r['disable-scroll']}}"
    hover-class="{{r['hover-class']}}"
    hover-start-time="{{r['hover-start-time']}}"
    hover-stay-time="{{r['hover-stay-time']}}"
    hidden="{{r.hidden}}"
    hover-stop-propagation="{{r['hover-stop-propagation']}}"
    role="{{r.role}}"
  >
    <template is="children" data="{{r: r.children}}" />
  </view>
</template>

<template name="swiper">
  <swiper
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onTouchMove="onTouchMove"
    onTouchStart="onTouchStart"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"

    onChange="onSwiperChange"
    onTransition="onSwiperTransition"
    onAnimationEnd="onSwiperAnimationEnd"

    indicator-dots="{{r['indicator-dots']}}"
    indicator-color="{{r['indicator-color']}}"
    indicator-active-color="{{r['indicator-active-color']}}"
    active-class="{{r['active-class']}}"
    changing-class="{{r['changing-class']}}"
    autoplay="{{r.autoplay}}"
    current="{{r.current}}"
    duration="{{r.duration}}"
    interval="{{r.interval}}"
    circular="{{r.circular}}"
    vertical="{{r.vertical}}"
    previous-margin	="{{r['previous-margin']}}"
    next-margin="{{r['next-margin']}}"
    acceleration="{{r.acceleration}}"
    disable-programmatic-animation="{{r['disable-programmatic-animation']}}"
    disable-touch="{{r['disable-touch']}}"
  >
    <swiper-item
      a:for="{{r.children}}"
      a:key="nodeId"
      style="{{item.style}}"
      class="{{item.class}}"
      id="{{item.id}}"
      data-private-node-id="{{item.nodeId}}"
      data-private-page-id="{{item.pageId}}"
    >
      <template is="children" data="{{r: item.children}}" />
    </swiper-item>
  </swiper>
</template>

<template name="scroll-view">
  <scroll-view
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onScrollToUpper="onScrollViewScrollToUpper"
    onScrollToLower="onScrollViewScrollToLower"
    onScroll="onScrollViewScroll"

    scroll-x="{{r['scroll-x']}}"
    scroll-y="{{r['scroll-y']}}"
    upper-threshold="{{r['upper-threshold']}}"
    lower-threshold="{{r['lower-threshold']}}"
    scroll-top="{{r['scroll-top']}}"
    scroll-left="{{r['scroll-left']}}"
    scroll-into-view="{{r['scroll-into-view']}}"
    scroll-with-animation="{{r['scroll-with-animation']}}"
    scroll-animation-duration="{{r['scroll-animation-duration']}}"
    enable-back-to-top="{{r['enable-back-to-top']}}"
    trap-scroll="{{r['trap-scroll']}}"
  >
    <template is="children" data="{{r: r.children}}" />
  </scroll-view>
</template>

<template name="cover-view">
  <cover-view
    style="{{r.style}}"
    class="{{r.class}}"
    id="{{r.id}}"
    hidden="{{r.hidden}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
  >
    <template is="children" data="{{r: r.children}}" />
  </cover-view>
</template>

<template name="cover-image">
  <cover-image
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"

    src="{{r.src}}"
  >
    <template is="children" data="{{r: r.children}}" />
  </cover-image>
</template>

<template name="moveable-area">
  <moveable-area
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    scale-area="{{r['scale-area']}}"
  >
    <moveable-view
      a:for="{{r.children}}"
      a:key="nodeId"
      a:if="{{item.nodeType !== 'comment'}}"
      style="{{item.style}}"
      class="{{item.class}}"
      hidden="{{item.hidden}}"
      id="{{item.id}}"
      data-private-node-id="{{item.nodeId}}"
      data-private-page-id="{{item.pageId}}"

      onTap="onTap"
      onTouchStart="onTouchStart"
      onTouchMove="onTouchMove"
      onTouchEnd="onTouchEnd"
      onTouchCancel="onTouchCancel"

      animation="{{r.animation !== undefined ? r.animation : true}}"
      direction="{{item.direction || 'none'}}"
      inertia="{{item.inertia}}"
      out-of-bounds="{{item['out-of-bounds']}}"
      x="{{item.x}}"
      y="{{item.y}}"
      damping="{{item.damping}}"
      friction="{{item.friction}}"
      disabled="{{item.disabled}}"
      scale="{{item.scale}}"
      scale-min="{{item['scale-min'] !== undefined ? item['scale-min'] : 0.5}}"
      scale-max="{{item['scale-max'] !== undefined ? item['scale-max'] : 10}}"
      scale-value="{{item['scale-value'] !== undefined ? item['scale-value'] : 1}}"
    >
      <template is="children" data="{{r: item.children}}" />
    </moveable-view>
  </moveable-area>
</template>

<!--Base content-->
<template name="text">
  <text
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    animation="{{r.animation}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onTouchMove="onTouchMove"
    onTouchStart="onTouchStart"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"

    decode="{{r.decode}}"
    space="{{r.space}}"
    decode="{{r.decode}}"
    number-of-lines="{{r['number-of-lines']}}"
  >
    <template is="children" data="{{r: r.children}}" />
  </text>
</template>

<template name="icon">
  <icon
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onTouchMove="onTouchMove"
    onTouchStart="onTouchStart"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"

    type="{{r.type}}"
    size="{{r.size !== undefined ? r.size : '23'}}"
    color="{{r.color}}"
  >
  </icon>
</template>

<template name="progress">
  <progress
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onTouchMove="onTouchMove"
    onTouchStart="onTouchStart"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"

    onActiveEnd="onProgressActiveEnd"

    percent="{{r.percent}}"
    show-info="{{r['show-info']}}"
    border-radius="{{r['border-radius']}}"
    font-size="{{r['font-size']}}"
    stroke-width="{{r['stroke-width']}}"
    color="{{r.color}}"
    active-color="{{r['active-color']}}"
    background-color="{{r['background-color']}}"
    active="{{r.active}}"
    active-mode="{{r['active-mode']}}"
  >
  </progress>
</template>

<template name="rich-text">
  <rich-text
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onTouchMove="onTouchMove"
    onTouchStart="onTouchStart"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"
    onLongTap="onLongTap"

    nodes="{{r.nodes}}"
  >
  </rich-text>
</template>

<!--Form-->
<template name="button">
  <button
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    animation="{{r.animation}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onTouchMove="onTouchMove"
    onTouchStart="onTouchStart"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"

    onGetUserInfo="onButtonGetUserInfo"
    onContact="onButtonContact"
    onGetPhoneNumber="onButtonGetPhoneNumber"
    onError="onButtonError"
    onOpenSetting="onButtonOpenSetting"
    onLaunchApp="onButtonLaunchApp"

    size="{{r.size || 'default'}}"
    type="{{r.type || 'default'}}"
    plain="{{r.plain || false}}"
    disabled="{{r.disabled || false}}"
    loading="{{r.loading || false}}"
    form-type="{{r['form-type'] || ''}}"
    open-type="{{r['open-type'] || ''}}"
    hover-class="{{r['hover-class'] || 'button-hover'}}"
    hover-stop-propagation="{{r['hover-stop-propagation'] || false}}"
    hover-start-time="{{r['hover-start-time'] !== undefined ? r['hover-start-time'] : 20}}"
    hover-stay-time="{{r['hover-stay-time'] !== undefined ? r['hover-stay-time'] : 70}}"
    lang="{{r.lang || 'en'}}"
    session-from="{{r['session-from'] || ''}}"
    send-message-title="{{r['send-message-title'] || ''}}"
    send-message-path="{{r['send-message-path'] || ''}}"
    send-message-img="{{r['send-message-img'] || ''}}"
    app-parameter="{{r['app-parameter'] || ''}}"
    show-message-card="{{r['show-message-card'] || ''}}"
    public-id="{{r['public-id'] || ''}}"
  >
    <template is="children" data="{{r: r.children}}" />
  </button>
</template>

<template name="form">
  <form
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onTouchMove="onTouchMove"
    onTouchStart="onTouchStart"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"

    onSubmit="onFormSubmit"
    onReset="onFormReset"

    report-submit="{{r['report-submit']}}"
    report-submit-timeout="{{r['report-submit-timeout'] || 0}}"
  >
    <template is="children" data="{{r: r.children}}" />
  </form>
</template>

<template name="label">
  <label
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onTouchMove="onTouchMove"
    onTouchStart="onTouchStart"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"

    for="{{r.for || ''}}"
  >
    <template is="children" data="{{r: r.children}}" />
  </label>
</template>

<template name="input">
  <input
    style="{{r.style}}"
    class="{{isHTMLElement ? `h5-input ${r.class}` : r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"

    onInput="onInputInput"
    onFocus="onInputFocus"
    onBlur="onInputBlur"
    onConfirm="onInputConfirm"
    onKeyboardHeightChange="onInputKeyBoardHeightChange"

    value="{{r.value}}"
    type="{{r.type}}"
    password="{{r.password}}"
    placeholder="{{r.placeholder}}"
    placeholder-style="{{r['placeholder-style']}}"
    placeholder-class="{{r['placeholder-class']}}"
    disabled="{{r.disabled}}"
    maxlength="{{r.maxlength}}"
    cursor-spacing="{{r['cursor-spacing']}}"
    focus="{{r['focus-state']}}"
    controlled="{{r.controlled}}"
    confirm-type="{{r['confirm-type']}}"
    confirm-hold="{{r['confirm-hold']}}"
    cursor="{{r.cursor}}"
    selection-start="{{r['selection-start']}}"
    selection-end="{{r['selection-end']}}"
    adjust-position="{{r['adjust-position']}}"
  >
  </input>
</template>

<template name="textarea">
  <textarea
    style="{{r.style}}"
    class="{{isHTMLElement ? `h5-textarea ${r.class}` : r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"

    onFocus="onTextareaFocus"
    onBlur="onTextareaBlur"
    onLineChange="onTextareaLineChange"
    onInput="onTextareaInput"
    onConfirm="onTextareaConfirm"
    onKeyboardHeightChange="onTextareaKeyBoardHeightChange"

    value="{{r.value}}"
    placeholder="{{r.placeholder}}"
    placeholder-style="{{r['placeholder-style']}}"
    placeholder-class="{{r['placeholder-class']}}"
    disabled="{{r.disabled}}"
    maxlength="{{r.maxlength}}"
    focus="{{r['focus-state']}}"
    controlled="{{r.controlled}}"
    auto-height="{{r['auto-height']}}"
    fixed="{{r.fixed}}"
    cursor-spacing="{{r['cursor-spacing']}}"
    cursor="{{r.cursor}}"
    show-confirm-bar="{{r['show-confirm-bar']}}"
    selection-start="{{r['selection-start']}}"
    selection-end="{{r['selection-end']}}"
    adjust-position="{{r['adjust-position']}}"
  >
  </textarea>
</template>

<template name="radio">
  <radio
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"

    value="{{r.value}}"
    disabled="{{r.disabled}}"
    checked="{{r.checked}}"
    color="{{r.color}}"
  >
    <template is="children" data="{{r: r.children}}" />
  </radio>
</template>

<template name="radio-group">
  <radio-group
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onChange="onRadioChange"

    name="{{name}}"
  >
    <template is="children" data="{{r: r.children}}" />
  </radio-group>
</template>

<template name="checkbox">
  <checkbox
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onChange="onCheckboxItemChange"

    name="{{r.name}}"
    value="{{r.value || ''}}"
    disabled="{{r.disabled}}"
    checked="{{r.checked}}"
    color="{{r.color}}"
  >
  </checkbox>
</template>

<template name="checkbox-group">
  <checkbox-group
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onChange="onCheckboxChange"

    name="{{r.name}}"
  >
    <template is="children" data="{{r: r.children}}" />
  </checkbox-group>
</template>

<template name="switch">
  <switch
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onChange="onSwitchChange"

    name="{{r.name}}"
    checked="{{r.checked}}"
    disabled="{{r.disabled}}"
    type="{{r.type}}"
    color="{{r.color}}"
  >
  </switch>
</template>

<template name="slider">
  <slider
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
    onChange="onSliderChange"
    onChanging="onSliderChanging"

    name="{{r.name}}"
    min="{{r.min}}"
    max="{{r.max}}"
    step="{{r.step}}"
    disabled="{{r.disabled}}"
    value="{{r.value}}"
    color="{{r.color}}"
    selected-color="{{r['selected-color']}}"
    active-color="{{r['active-color']}}"
    background-color="{{r['background-color']}}"
    block-size="{{r['block-size']}}"
    block-color="{{r['block-color']}}"
    show-value="{{r['show-value']}}"
  >
  </slider>
</template>

<template name="picker-view">
  <picker-view
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onChange="onPickerViewChange"
    onPickStart="onPickerViewPickstart"
    onPickEnd="onPickerViewPickend"

    indicator-style="{{r['indicator-style'] || ''}}"
    indicator-class="{{r['indicator-class'] || ''}}"
    mask-style="{{r['mask-style'] || ''}}"
    mask-class="{{r['mask-class'] || ''}}"
    value="{{r.value || []}}"
  >
    <picker-view-column
      a:for="{{r.children}}"
      a:key="nodeId"

      style="{{item.style}}"
      class="{{item.class}}"
      hidden="{{item.hidden}}"
      id="{{item.id}}"
      data-private-node-id="{{item.nodeId}}"
      data-private-page-id="{{item.pageId}}"

      onTouchStart="onTouchStart"
      onTouchMove="onTouchMove"
      onTouchEnd="onTouchEnd"
      onTouchCancel="onTouchCancel"
      onTap="onTap"
    >
      <view a:for="{{item.children}}" a:for-item="pickerColumnItem">
        <block a:if="{{pickerColumnItem.nodeType === 'text'}}">
          {{pickerColumnItem.content}}
        </block>
        <block a:elif="{{pickerColumnItem.tagName === 'custom-component'}}">
          <custom-component r="{{pickerColumnItem}}">
            <template is="children" data="{{r: pickerColumnItem.children}}" />
          </custom-component>
        </block>
        <block a:else>
          <template is="{{pickerColumnItem._behavior || 'element'}}" data="{{r: pickerColumnItem}}" />
        </block>
      </view>
    </picker-view-column>
  </picker-view>
</template>

<template name="picker">
  <picker
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTouchStart="onTouchStart"
    onTouchMove="onTouchMove"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"
    onTap="onTap"

    onChange="onPickerChange"
    onCancel="onPickerCancel"

    name="{{r.name}}"
    disabled="{{r.disabled}}"
    range="{{r.range || []}}"
    range-key="{{r['range-key'] || ''}}"
    value="{{r.value}}"
  >
    <view><template is="children" data="{{r: r.children}}" /></view>
  </picker>
</template>

<template name="navigator">
  <navigator
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTouchStart="onTouchStart"
    onTouchMove="onTouchMove"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"
    onTap="onTap"

    onSuccess="onNavigatorSuccess"
    onFail="onNavigatorFail"
    onComplete="onNavigatorComplete"

    url="{{r.url || ''}}"
    open-type="{{r['open-type'] || 'navigate'}}"
    hover-class="{{r['hover-class'] || 'none'}}"
    hover-stop-propagation="{{r['hover-stop-propagation']}}"
    hover-start-time="{{r['hover-start-time'] !== undefined ? r['hover-start-time'] : 50}}"
    hover-stay-time="{{r['hover-stay-time'] !== undefined ? r['hover-stay-time'] : 600}}"
  >
    <template is="children" data="{{r: r.children}}" />
  </navigator>
</template>

<template name="image">
  <image
    style="{{r.style}}"
    class="{{isHTMLElement ? `h5-img ${r.class}` : r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTouchStart="onTouchStart"
    onTouchMove="onTouchMove"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"
    onTap="onTap"

    onLoad="onImageLoad"
    onError="onImageError"

    rendering-mode="{{r['rendering-mode']}}"
    src="{{r.src}}"
    mode="{{r.mode || 'scaleToFill'}}"
    lazy-load="{{r['lazy-load']}}"
    default-source="{{r['default-source']}}"
  >
  </image>
</template>

<template name="video">
  <video
    style="{{r.style}}"
    class="{{isHTMLElement ? `h5-video ${r.class}` : r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTouchStart="onTouchStart"
    onTouchMove="onTouchMove"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"
    onTap="onTap"

    onPlay="onVideoPlay"
    onPause="onVideoPause"
    onEnded="onVideoEnded"
    onTimeUpdate="onVideoTimeUpdate"
    onFullScreenChange="onVideoFullScreenChange"
    onWaiting="onVideoWaiting"
    onError="onVideoError"
    onProgress="onVideoProgress"

    src="{{r.src}}"
    duration="{{r.duration}}"
    controls="{{r.controls}}"
    danmu-list="{{r['danmu-list']}}"
    danmu-btn="{{r['danmu-btn']}}"
    enable-danmu="{{r['enable-danmu']}}"
    autoplay="{{r.autoplay}}"
    loop="{{r.loop}}"
    muted="{{r.muted}}"
    initial-time="{{r['initial-time']}}"
    direction="{{r.direction}}"
    show-progress="{{r['show-progress']}}"
    show-fullscreen-btn="{{r['show-fullscreen-btn']}}"
    show-play-btn="{{r['show-play-btn']}}"
    show-center-play-btn="{{r['show-center-play-btn']}}"
    enable-progress-gesture="{{r['enable-progress-gesture']}}"
    object-fit="{{r['object-fit']}}"
    poster="{{r.poster}}"
    show-mute-btn="{{r['show-mute-btn']}}"
    title="{{r.title}}"
    play-btn-position="{{r['play-btn-position']}}"
    enable-play-gesture="{{r['enable-play-gesture']}}"
    auto-pause-if-navigate="{{r['auto-pause-if-navigate']}}"
    auto-pause-if-open-native="{{r['auto-pause-if-open-native']}}"
    vslide-gesture="{{r['vslide-gesture']}}"
    vslide-gesture-in-fullscreen="{{r['vslide-gesture-in-fullscreen']}}"
    enableNative="{{r.enableNative}}"
    playsinline="{{r.playsinline}}"
    rawControls="{{r.rawControls}}"
  >
  </video>
</template>

<template name="map">
  <map
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTouchStart="onTouchStart"
    onTouchMove="onTouchMove"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"
    onTap="onTap"

    onMarkerTap="onMapMarkerTap"
    onControlTap="onMapControlTap"
    onCalloutTap="onMapCalloutTap"
    onUpdated="onMapUpdated"
    onRegionChange="onMapRegionChange"

    longitude="{{r.longitude !== undefined ? r.longitude : 39.92}}"
    latitude="{{r.latitude !== undefined ? r.latitude : 116.46}}"
    scale="{{r.scale !== undefined ? r.scale : 16}}"
    markers="{{r.markers !== undefined ? r.markers : []}}"
    polyline="{{r.polyline !== undefined ? r.polyline : []}}"
    circles="{{r.circles !== undefined ? r.circles : []}}"
    controls="{{r.controls !== undefined ? r.controls : []}}"
    include-points="{{r['include-points'] !== undefined ? r['include-points'] : []}}"
    show-location="{{r['show-location']}}"
    polygons="{{r.polygons !== undefined ? r.polygons : []}}"
    subkey="{{r.subkey || ''}}"
    layer-style="{{r['layer-style'] !== undefined ? r['layer-style'] : 1}}"
    rotate="{{r.rotate || 0}}"
    skew="{{r.skew || 0}}"
    enable-3D="{{r['enable-3D']}}"
    show-compass="{{r['show-compass']}}"
    enable-overlooking="{{r['enable-overlooking']}}"
    enable-zoom="{{r['enable-zoom']}}"
    enable-scroll="{{r['enable-scroll']}}"
    enable-rotate="{{r['enable-rotate']}}"
    enable-satellite="{{r['enable-satellite']}}"
    enable-traffic="{{r['enable-traffic']}}"
  >
    <template is="children" data="{{r: r.children}}" />
  </map>
</template>

<template name="canvas">
  <canvas
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTouchStart="onTouchStart"
    onTouchMove="onTouchMove"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"
    onTap="onTap"

    onLongTap="onCanvasLongTap"
    onError="onCanvasError"

    id="{{r['canvas-id']}}"
    disable-scroll="{{r['disable-scroll']}}"
    width="{{r['width']}}"
    height="{{r['height']}}"
  >
  </canvas>
</template>

<template name="web-view">
  <web-view
    style="{{r.style}}"
    class="{{r.class}}"
    hidden="{{r.hidden}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTouchStart="onTouchStart"
    onTouchMove="onTouchMove"
    onTouchEnd="onTouchEnd"
    onTouchCancel="onTouchCancel"
    onTap="onTap"

    onMessage="onWebviewMessage"
    onLoad="onWebviewLoad"
    onError="onWebviewError"

    src="{{r.src}}"
  >
  </web-view>
</template>
