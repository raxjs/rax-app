<template name="element">
  <view
    style="{{r.style}}"
    class="h5-{{r.tagName}} {{r.className}}"
    id="{{r.id}}"
    data-private-node-id="{{r.nodeId}}"
    data-private-page-id="{{r.pageId}}"

    onTap="onTap"
  >
    <block a:for="{{r.children}}" a:key="nodeId">
      <block a:if="{{r.type === 'text'}}">
        {{r.content}}
      </block>
      <block a:else>
        <template is="element" data="{{r: item}}" />
      </block>
      <block>
    </block>
  </view>
</template>
